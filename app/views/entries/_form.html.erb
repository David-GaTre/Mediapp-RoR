<div class="row">
  <div class="col-md-6">
  <%= form_with(model: entry) do |form| %>
    <% if entry.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

        <ul>
          <% entry.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    
    <% patients_array = @patients.map { |p| [p.user.full_name, p.id] } %>
    <p style="font-family:verdana;font-size:17px;"> <strong><%= form.label :patient_id, "Paciente",class: "form-label" %></strong></p>
    <%= form.select :patient_id, options_for_select(patients_array),{}, :class => "form-control" %>
    <p style="font-family:verdana;font-size:17px;"> <strong><%= form.label :entry_type, "Tipo de Entrada ",class: "form-label" %></strong></p>
    <%= form.select :entry_type, options_for_select([ [], [ "Consulta", "consultation"], ["Hospitalización", "hospitalization"] ], ""),{}, :class => "form-control" %>

    <p style="font-family:verdana;font-size:17px;"><strong><%= form.label :comments, "Comentarios ",class: "form-label" %></strong></p>
    <%= form.text_area :comments, class: "form-control" %>

    <%= form.hidden_field :office_id, value: entry.office_id %>
    

    <br>
    <div class="actions">
    <p style="font-family:verdana">  <%= form.submit 'Crear', class: "btn btn-secondary" %></p
    </div>
    </div>
  <% end %>
  </div>
</div>
